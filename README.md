# gsheet

Stata program to directly import a public Google Sheet by URL using the Stata Python Integration

Requires a working Python instance correctly setup in Stata 16+ with a functioning numpy package
